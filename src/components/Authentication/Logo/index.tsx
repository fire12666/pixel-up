import styled from "@emotion/styled"

const Logo = () => {
    return (
        <Container>
            <h2>Logo</h2>
        </Container>
    )
}

const Container = styled('div')`
    flex: 1
`

export default Logo